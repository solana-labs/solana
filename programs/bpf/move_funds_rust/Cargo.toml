[package]
name = "move_funds_rust"
version = "0.1.0"
authors = ["Jack May <jack@solana.com>"]

[dependencies]
bincode = "1.0.0"
generic-array = { version = "0.12.0", default-features = false, features = ["serde"] }
solana_program_interface = { path = "../../../common" }