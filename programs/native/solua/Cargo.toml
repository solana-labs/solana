[package]
name = "solua"
version = "0.1.0"
authors = ["Solana Maintainers <maintainers@solana.com>"]

[dependencies]
rlua = "0.15.2"
solana_program_interface = { path = "../../../common" }

[dev-dependencies]
bincode = "1.0.0"

[lib]
name = "solua"
crate-type = ["dylib"]

