[package]
name = "noop"
version = "0.1.0"
authors = ["Solana Maintainers <maintainers@solana.com>"]

[dependencies]
solana_program_interface = { path = "../../../common" }

[lib]
name = "noop"
crate-type = ["cdylib"]

