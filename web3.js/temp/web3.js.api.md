## API Report File for "@solana/web3.js"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import BN from 'bn.js';

// Warning: (ae-internal-missing-underscore) The name "BLOCKHASH_CACHE_TIMEOUT_MS" should be prefixed with an underscore because the declaration is marked as @internal
//
// @internal
export const BLOCKHASH_CACHE_TIMEOUT_MS: number;

// @public
export type Commitment = 'max' | 'recent' | 'root' | 'single' | 'singleGossip';

// @public
export class Connection {
    constructor(endpoint: string, commitment?: Commitment);
    getSignatureStatuses(signatures: Array<TransactionSignature>, config?: SignatureStatusConfig): Promise<RpcResponseAndContext<Array<SignatureStatus | null>>>;
    // Warning: (ae-forgotten-export) The symbol "PublicKey" needs to be exported by the entry point index.d.ts
    requestAirdrop(to: PublicKey, amount: number): Promise<TransactionSignature>;
    }

// @public
export interface EpochInfo {
    absoluteSlot: number;
    blockHeight?: number;
    epoch: number;
    slotIndex: number;
    slotsInEpoch: number;
    transactionCount?: number;
}

// @public
export interface EpochSchedule {
    firstNormalEpoch: number;
    firstNormalSlot: number;
    leaderScheduleSlotOffset: number;
    slotsPerEpoch: number;
    warmup: boolean;
}

// @public
export interface InflationGovernor {
    foundation: number;
    foundationTerm: number;
    initial: number;
    taper: number;
    terminal: number;
}

// @public
export interface PerfSample {
    numSlots: number;
    numTransactions: number;
    samplePeriodSecs: number;
    slot: number;
}

// @public
export interface RecentBlockhash {
    // Warning: (ae-forgotten-export) The symbol "Blockhash" needs to be exported by the entry point index.d.ts
    blockhash: Blockhash;
    // Warning: (ae-forgotten-export) The symbol "FeeCalculator" needs to be exported by the entry point index.d.ts
    feeCalculator: FeeCalculator;
}

// @public
export type RpcResponseAndContext<T> = {
    context: {
        slot: number;
    };
    value: T;
};

// @public
export interface SignatureStatus {
    confirmations: number | null;
    confirmationStatus?: string | null;
    err: TransactionError | null;
    slot: number;
}

// @public
export interface SignatureStatusConfig {
    searchTransactionHistory: boolean;
}

// @public
export type TransactionError = object;

// @public
export type TransactionSignature = string;

// @public
export interface Version {
    'feature-set': number | null;
    'solana-core': string;
}


// (No @packageDocumentation comment for this package)

```
